# Observations

Each JSON file is one explicitly contributed, privacy-reviewed launch attempt.
Files are partitioned by observation year and month. The public record uses a
random per-run UUID and never a persistent contributor identifier.

There are intentionally no seed observations. Historical prose evidence in a
recipe does not become structured benchmark data retroactively.
