name = "podhammer-truth-intake"
main = "src/index.ts"
compatibility_date = "2026-08-01"

[[d1_databases]]
binding = "DB"
database_name = "podhammer-truth"
database_id = "REPLACE_AFTER_D1_CREATE"

# Add secrets after deployment; never commit their values:
# npx wrangler secret put INTAKE_PEPPER
# npx wrangler secret put EXPORT_TOKEN
